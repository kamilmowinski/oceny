import {
  it,
  inject,
  injectAsync,
  beforeEachProviders,
  TestComponentBuilder
} from 'angular2/testing';
import {Grade} from './grade';

describe('Grade', () => {
  let grade;

  beforeEach(() => {

  });

  it('should return the list of grades', () => {

  });
});
